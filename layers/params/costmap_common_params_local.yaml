map_type: costmap
global_frame: odom # xb20 onboard
robot_base_frame: BASE

transform_tolerance: 1
meter_scoring: true

#机器人外形
footprint: [[-0.65, -0.22], [-0.65, 0.22], [0.45, 0.22], [0.45, -0.22]]
# footprint: [[-0.64, -0.40], [-0.64, 0.40], [0.64, 0.40], [0.64, -0.40]]
footprint_padding: 0.0

plugins:
  - { name: elevation_layer, type: "costmap_2d::ElevationMapLayer" }
  - { name: inflation_layer, type: "costmap_2d::InflationLayer" }

elevation_layer:
  source_topic: "/robot_Z/visual_map"
  travers_thresh: 0.8

inflation_layer:
  enabled: true
  inflation_radius: 0.20 # 碰撞半径
  cost_scaling_factor: 0.0

static_layer:
  enabled: false #不希望使用静态的地图，因为不利于局部避障
  trinary_costmap: false
